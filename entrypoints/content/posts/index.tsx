import React from 'react';

interface PostModalProps {
  posts: any;
  onRemove: () => void;
}

const PostModal = ({ posts, onRemove }: PostModalProps) => {
  return <div>PostModal</div>;
};

export default PostModal;
